name = "jofi-music"

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "nodejs_22"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -r requirements.txt",
  "cd app && npm install && npm run build"
]

[start]
cmd = "python server.py"
